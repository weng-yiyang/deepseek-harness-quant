# 复制为 .credentials.yaml 并填入你的 DeepSeek API Key：
#   Windows: copy .credentials.yaml.example .credentials.yaml
#   Linux:   cp .credentials.yaml.example .credentials.yaml
# 获取：https://platform.deepseek.com/api_keys
# 提示：先接入 DeepSeek API Key，启动后即可让 AI 助手协助你接入其余数据源 API
#（Tushare token 等，见 docs/HARNESS接入.md）
DEEPSEEK_API_KEY: <your-deepseek-api-key>
